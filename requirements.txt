fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
google-generativeai==0.8.3
python-dotenv==1.2.3

# Local OCR fallback, used when Gemini fails or hits quota (see ocr_extractor.py).
# Also requires the Tesseract OCR engine installed on the machine — see .env.example.
pytesseract==0.3.13
Pillow==10.4.0
PyMuPDF==1.24.11
